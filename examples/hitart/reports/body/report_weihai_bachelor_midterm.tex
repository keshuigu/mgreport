\section{课题主要研究内容及进度}
\subsection{课题主要研究内容}
近年来，图像识别领域中深度神经网络被广泛时使用。与之伴随的安全问题也逐渐受到人们的关注。本课题主要关注图像识别领域中的两类比较有代表性的模型，探讨其相关的隐私泄露问题。

第一类模型是基于模板（Template）匹配的识别模型，主要应用于人脸识别、指纹识别等生物特征识别系统。此类模型用户的原始数据如人脸图像 $x$ ，经过特征提取网络 $F(\cdot)$ 得到特征模板 $t = F(x)$，并存储于数据库，这里的 $t$ 称为模板信息。在需要进行身份验证时，用户给出新的输入图像 $x'$ ；识别系统会比对此次输入的图像与数据库中的特征模板的匹配程度，从而判断用户身份。以常见的余弦相似度为例，模板匹配的判定准则为：
\[
  d(F(x'), t) = \frac{F(x') \cdot t}{\| F(x') \|_2 \| t \|_2}
\]
若 $d(F(x'), t) > \tau$，则判定 $x'$ 与模板 $t$ 属于同一身份，其中 $\tau$ 为系统设定的阈值。

虽然这种方法在实际应用中具有较高的准确率和效率，但也存在隐私泄露的风险。具体来说，由于模板存储在特征数据库中，假设攻击者获得了特征数据库的访问权限，窃取到特征模板信息，则攻击者可以通过模板信息反向生成出接近用户的图像信息，从而误导识别模型的识别结果。针对这类模型的攻击方法称为模板反演攻击(Template Inversion Attack，TIA)。模板反演攻击的核心在于利用特征模板 $t$ 来重建与之匹配的原始输入图像 $x'$ 。攻击者可以通过优化算法或生成模型来实现这一目标。此类攻击可通过如下优化目标来描述：

\[
  x^* = \arg\min_{x'} \| F(x') - t \|_2^2
\]

即寻找一个输入 $x^*$，使其特征与模板 $t$ 尽可能接近。

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

第二类模型是图像分类模型，主要应用于图像分类、目标检测等任务。此类模型通常包含与基于模板匹配的模型类似的提取特征的网络层，与其不同之处在于图像分类模型在提取特征后包含额外的全连接层，将提取的特征值转化为置信度分布，输出每个类别的概率分布，以此来达到分类的目的。图像分类模型的工作流程如下：
\begin{enumerate}
  \item 输入图像 $x$ 经过特征提取网络 $f(\cdot)$，得到特征向量 $t = f(x)$。
  \item 特征向量 $t$ 经过全连接层和激活函数，输出类别概率分布 $p = g(t)$。
  \item 根据最大概率进行分类决策 $\hat{y} = \arg\max p$。
\end{enumerate}

针对此类模型，攻击者的目的时通过对模型参数的分析，推断出训练数据的分布和特征，从而恢复用户的隐私信息。与这类模型相关的攻击一般被称为模型反演攻击(Model Inversion Attacks，MIAs)，其目的是利用"目标分类器"来重构私有训练样本。在本课题中，我们重点研究白盒情境下的模型反演攻击。在这种情况下，攻击者被假设可以完全访问目标分类器，攻击者拟定一个类别，利用模型参数和训练数据的分布信息，推断出训练该模型对应该类别训练所使用的图像。具体来说，假设已知模型 $F$ 及其参数，攻击者通过优化或生成方法，寻找一个输入 $x^*$，使得模型输出满足属于某一特定类别，从而重建或推断出训练数据的敏感信息。设 $F(x)$ 为模型对输入 $x$ 的分类概率输出，$y_t$ 为目标类别。攻击者的目标是找到一个输入 $x^*$，使得 $F(x^*)$ 在目标类别上的概率最大：
\[
  x^* = \arg\max_{x} \; f_\theta(x)_{y_t}
\]

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

目前常见的利用生成模型进行模板反演攻击的方法主要有两种：一种是基于生成对抗网络（GAN）的生成模型，另一种是基于扩散模型的生成模型。GAN通过对抗训练生成与目标模板匹配的图像，而扩散模型则通过逐步去噪重建图像。本课题将重点研究基于扩散模型的模板反演攻击方法和模型反演攻击方法，利用扩散模型强大的生成能力，设计高效的模板反演攻击和模型反演攻击算法。

本课题中所使用的扩散模型主要基于明晰扩散模型（Elucidated Diffusion Models，EDM）的原理设计，EDM是扩散概率模型领域的重要进展，由Karras等人在2022年提出。EDM对扩散过程的噪声调度、采样策略和损失函数进行了系统性分析和优化，极大提升了扩散模型的采样效率和生成质量。

具体来说，扩散模型通过逐步向数据添加噪声，将数据分布映射为高斯分布，并训练神经网络学习逆过程以去噪重建数据。EDM提出了一种统一的噪声调度框架，定义了噪声幅度 $\sigma$ 的连续变化，并用参数化的去噪器 $f_\theta(x, \sigma)$ 预测无噪声数据。

EDM的核心训练目标为：
\[
  \mathcal{L}(\theta) = \mathbb{E}_{x_0, \sigma, \epsilon} \left[ w(\sigma) \left\| f_\theta(x_0 + \sigma \epsilon, \sigma) - x_0 \right\|^2 \right]
\]
其中，$x_0$ 为真实样本，$\epsilon \sim \mathcal{N}(0, I)$ 为高斯噪声，$\sigma$ 为噪声幅度，$w(\sigma)$ 为权重函数。
EDM提出的采样器（如二阶采样器）大幅减少了采样步数，提升了生成速度。EDM框架统一了多种扩散模型的训练与采样方式，便于理论分析和工程实现。通过合理的噪声调度和损失加权，EDM在图像生成等任务上取得了极高的保真度。

EDM将扩散过程建模为噪声幅度的连续变化，采样过程可用如下SDE（随机微分方程）描述：
\[
  dx = -\frac{1}{2} \sigma^2 \nabla_x \log p_\theta(x, \sigma) \, d\sigma + \sigma \, dW
\]

其中 $dW$ 为维纳过程，$\nabla_x \log p_\theta(x, \sigma)$ 由神经网络近似。

本课题使用EDM扩散模型作为基础，设计模板反演攻击和模型反演攻击的生成模型。通过对EDM的训练和优化，利用不同任务信息来重建匹配的原始输入图像，从而实现对用户隐私信息的还原。

\subsection{进度介绍}
截至目前，已完成课题相关的文献调研和相关理论的学习，确定了研究方案，搭建了实验环境。针对模板反演攻击，已实现了基于扩散模型的初步攻击方法，并对其有效性进行了初步实验验证，收集并分析了实验结果。

\section{目前已完成的主要研究工作及结果}

针对TIA和MIA的研究，已完成以下工作：

（1）文献调研：查阅了大量关于模板反演攻击和模型反演攻击的相关文献，了解了当前研究现状和技术难点。深入学习了图像识别模型的工作原理，包括特征提取和分类方法，为后续研究打下基础。

（2）实验环境搭建：搭建了实验所需的软硬件环境，包括深度学习框架和必要的计算资源。

（3）初步实验：对模板反演攻击进行了初步实验，验证了结合扩散生成模型进行攻击的可行性，并收集了初步实验结果数据。

\subsection{模板反演攻击的研究进展}
针对TI的隐私泄露问题，设计了如下的网络结构和攻击流程，其中扩散模型的核心部分采用了EDM扩散模型，利用其生成能力和噪声调度机制，设计高效的模板反演攻击算法。通过对扩散模型的训练和优化，重建与目标模板匹配的原始输入图像，从而实现对用户隐私信息的还原。

利用edm扩散模型进行模板反演攻击的训练和推理流程如下图所示：

\begin{figure}[!htbp]
  \centering
  \includegraphics[width=0.8\textwidth]{images/train.drawio.pdf}
  \caption{EDM扩散模型模板反演攻击训练流程示意图}
  \label{fig:edm_tia_train}
\end{figure}



%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
% xelatex -shell-escape report.tex
% bibtex report
% xelatex -shell-escape report.tex
% xelatex -shell-escape report.tex
以EDM扩散模型为基础，训练中使用的损失函数可以概括为：

$$
  \mathcal{L}(\theta) = \mathbb{E}_{x_0, \sigma, \epsilon} \left[ w(\sigma) \left\|  f_\theta(x_0 + \sigma \epsilon,y,\sigma) - x_0 \right\|^2 \right]
  + \lambda \| F( f_\theta(x_0 + \sigma \epsilon,y,\sigma)) - F(x_0) \|^2
$$

其中 $F(\cdot)$ 表示特征提取网络，$y$ 为目标类别标签，$\lambda$ 为平衡系数。
$\lambda$的作用是平衡生成图像与特征模板之间的相似度和生成图像与原始图像之间的相似度，使得生成的图像既能满足模板匹配的要求，又能尽可能接近原始输入图像。该参数需要随着训练的进行调整，以便在不同阶段优化模型的生成效果。

\begin{algorithm}[H]
\caption{模板反演攻击模型训练}
\begin{algorithmic}[1]
\STATE 初始化扩模型参数 $f_\theta$ ，加载特征提取网络$F$
\FOR{迭代 $=1$ 到 $N$}
    \STATE 随机采样一批训练样本 $(x_0, y_0)$
    \FOR{每个样本 $(x_0, y_0)$}
        \STATE 采样噪声 $\epsilon \sim \mathcal{N}(0, I)$，噪声幅度 $\sigma$
        \STATE 计算带噪输入 $x = x_0 + \sigma \epsilon$
        \STATE 生成图像 $\hat{x} = f_\theta(x, y_0, \sigma)$
        \STATE 计算损失 $\mathcal{L}(\theta) = \mathbb{E}_{x_0, \sigma, \epsilon} \left[ w(\sigma) \left\|  f_\theta(x_0 + \sigma \epsilon,y_0,\sigma) - x_0 \right\|^2 \right]
  + \lambda \| F( f_\theta(x_0 + \sigma \epsilon,y_0,\sigma)) - F(x_0) \|^2$
    \ENDFOR
    \STATE 更新 $\theta$以最小化 $\mathcal{L}(\theta)$
\ENDFOR
\end{algorithmic}
\end{algorithm}

训练中损失函数变化如图2所示：

\begin{figure}[htbp]
  \centering
  \includegraphics[width=0.8\textwidth]{images/loss_plot.png}
  \caption{训练过程Loss变化曲线}
  \label{fig:edm_template_attack_loss}
\end{figure}


在训练完毕后，模型可以用于推理阶段，即利用训练好的扩散模型生成与目标特征模板匹配的图像。推理过程如下图所示：

\begin{figure}[!htbp]
  \centering
  \includegraphics[width=0.8\textwidth]{images/infer.drawio.pdf}
  \caption{EDM扩散模型模板反演攻击推理流程示意图}
  \label{fig:edm_tia_infer}
\end{figure}

实验设置：

本实验采用公开的人脸图像数据集 CelebA 作为基础数据集。CelebA 数据集包含大量多样化的人脸图像，广泛用于人脸识别和生成模型等相关研究。实验中，选取了其中部分样本用于模型训练和评估。

在攻击目标模型的选择上，本文采用了当前主流的人脸识别模型 ArcFace，具体为 ArcFace-R100 结构。ArcFace 模型以其高精度的人脸特征提取能力被广泛应用于实际场景。实验过程中，首先利用 ArcFace-R100 模型对 CelebA 数据集中的每一张图像进行特征提取，获得对应的特征模板。随后，将原始图像与其特征模板配对，构建成用于训练和攻击的完整数据集。

训练进行了 482,000 次迭代后，对损失函数中的平衡系数 $\lambda$ 进行了调整。$\lambda$ 控制生成图像与特征模板之间的相似度约束与生成图像与原始输入图像之间的相似度约束之间的权重分配。调整后，降低了对特征模板的依赖程度，从而使得生成模型更加关注还原原始输入图像的细节和整体结构。通过这种动态调整，有助于提升最终生成图像的质量，使其在保持与特征模板一致性的同时，更加接近真实的人脸图像分布。


实验得到以下结果：
\begin{itemize}
  \item \textbf{识别准确率}：在特征数据库完整的情况下，模板反演攻击的识别准确率达到了85\%以上，表明该方法在特定条件下具有较强的隐私泄露能力。
  \item \textbf{重建效果}：通过优化算法，能够有效重建与模板匹配的原始输入图像，重建质量较高。
  \item \textbf{抗干扰性}：在添加噪声或对抗样本的情况下，模板反演攻击的效果有所下降，但仍能保持一定的识别能力。
\end{itemize}

生成图像的结果如下图所示。可以看出，所提出的方法能够有效地重建与目标特征模板相匹配的人脸图像。生成的人脸图像在整体结构、面部特征等方面与原始输入图像高度相似，且具备较高的视觉质量。

\subsection{模型反演攻击的研究进展}
设计了训练流程，模型反演攻击的训练流程与模板反演攻击类似，主要区别在于损失函数的设计和目标类别的选择。模型反演攻击的目标是通过优化算法重建与目标类别匹配的原始输入图像。模型反演攻击的训练流程如下图所示：

\begin{figure}[!htbp]
  \centering
  \includegraphics[width=0.8\textwidth]{images/train_mia.drawio.pdf}
  \caption{EDM扩散模型模型反演攻击训练流程示意图}
  \label{fig:edm_mia_train}
\end{figure}

推理过程需要迭代图像生成和模型输出的置信度标签，直到生成的图像满足目标类别的要求。推理流程如下图所示：


\begin{figure}[!htbp]
  \centering
  \includegraphics[width=0.8\textwidth]{images/infer_mia.drawio.pdf}
  \caption{EDM扩散模型模型反演攻击推理流程示意图}
  \label{fig:edm_mia_infer}
\end{figure}

\section{后期拟完成的研究工作及进度安排}
\subsection{后期拟完成的研究工作}
后期拟完成的研究工作主要包括以下几个方面：

（1）设计结合生成模型的模型反演攻击方法。MIA将结合EDM扩散模型进行实现，重点研究如何通过优化算法重建与目标类别匹配的原始输入图像，设计合适的损失函数以充分利用白盒场景下目标模型的梯度以及输出的置信度标签。

（2）完善模板反演攻击的实验设置，进一步优化模型效果，提高模板匹配的准确性以及生成图片的质量。

（3）进行大规模实验，验证模板反演攻击和模型逆向攻击在不同数据集和模型架构下的效果，分析其隐私泄露的风险。

（4）撰写相关论文，整理研究成果，归纳总结模板反演攻击和模型逆向攻击的隐私泄露问题。



\subsection{后期进度安排}

根据后期拟完成的研究工作，进度安排如下表所示：

\begin{table}[htbp]
  \centering
  \begin{tabular}{|c|c|}
    \hline
    时间节点     & 主要任务  \\
    \hline
    2025年07月 & 设计结合生成模型的模型反演攻击方法，实现相关代码 \\
    \hline
    2025年08月 & 进行实验并分析结果，优化模型结果 \\
    \hline
    2025年09月 & 进行大规模实验，验证两种攻击在不同数据集和模型架构下的效果 \\
    \hline
    2025年11月 & 撰写论文初稿 \\
    \hline
    2025年12月 & 完善论文，准备答辩材料 \\
    \hline
  \end{tabular}
  \caption{后期研究进度安排表}
\end{table}

% ...existing

\section{存在的问题与困难}
目前主要存在以下问题：

（1）目前已有的攻击模型所生成的图片质量不佳，需合理调整参数以平衡攻击准确率与图像质量。
现有模型在依据特征值重建用户图像时，往往出现图像模糊、细节缺失甚至于人脸丢失等现象，影响了攻击的实际效果。
为此，需要进一步探索和优化生成模型的结构与损失函数设计，尝试不同的超参数组合，并结合实验结果动态调整，以在保证攻击准确率的同时，提升生成图片的清晰度和真实性。
此外，还可以考虑引入更先进的生成模型或后处理方法，以进一步改善生成图片的质量。

（2）生成式模型训练需消耗大量算力，训练缓慢，时间安排较为紧张，需要合理规划后续工作，并行安排多项任务以提高效率。
由于生成式模型参数众多、训练过程复杂，模型训练和调优往往需要较长时间，容易导致整体进度受限。
为此，后续工作中应充分利用现有计算资源，合理安排实验顺序，将模板反演攻击的大规模实验任务与模型反演攻击的实现调优等任务并行推进。

\section{如期完成论文按时完成的可能性}
根据目前的研究进展和后续工作计划，结合已有的研究基础和资源条件，论文如期完成具有较高的可能性。具体分析如下：

目前已完成相关文献调研、研究方案设计及部分实验，为后续工作奠定了坚实基础。后续研究工作已细化为若干阶段，研究方法和技术路线已基本确定，并制定了详细的时间表，能够有效保障各项任务的顺利推进。对可能出现的技术难题和进度风险已提前预判，并制定了相应的应对措施。

综上所述，在科学合理的计划和充分的准备下，论文有望按时高质量完成。
% \section{参考文献}
% \bibliographystyle{hithesis}
% \bibliography{reference}

% Local Variables:
% TeX-master: "../mainart"
% TeX-engine: xetex
% End: